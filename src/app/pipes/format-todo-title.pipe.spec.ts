import { FormatTodoTitlePipe } from './format-todo-title.pipe';

describe('FormatTodoTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTodoTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
